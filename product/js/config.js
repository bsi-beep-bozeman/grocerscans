/* Populated after `supabase start` — see product/README.md.
   For local dev, values come from `supabase status`. */

export const SUPABASE_URL = 'http://127.0.0.1:54321';
export const SUPABASE_ANON_KEY = '__replace_after_supabase_start__';
